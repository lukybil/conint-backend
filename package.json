{
  "name": "semester-project",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./production/server.js",
    "dockerize": "npm run build && docker build -t conint-backend:latest .",
    "start docker": "docker run -p 10001:10001 conint-backend:latest",
    "test": "npm run build && jest --testPathPattern=./production"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.3",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.23",
    "@types/supertest": "^2.0.12",
    "jest": "^28.0.2",
    "supertest": "^6.2.3"
  }
}
